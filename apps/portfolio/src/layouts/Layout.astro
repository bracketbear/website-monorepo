---
import { LayeredLayout } from '@bracketbear/core';
import { navigationConfig } from '@/config/navigation';
import { makeTitle } from '@/config/page-meta';
import '@/styles/global.css';

interface Props {
  title?: string;
  hideContactForm?: boolean;
  hideFooter?: boolean;
  hideNavigation?: boolean;
  breadcrumbs?: any;
}

const { title, hideContactForm, hideFooter, hideNavigation, breadcrumbs } =
  Astro.props;

const pageTitle = makeTitle(title);
---

<LayeredLayout
  title={pageTitle}
  hideContactForm={hideContactForm}
  hideFooter={hideFooter}
  hideNavigation={hideNavigation}
  navigation={navigationConfig}
  breadcrumbs={breadcrumbs}
>
  <div slot="background">
    <!-- Background effects can go here -->
  </div>

  <slot />

  <div slot="foreground">
    <!-- <PointerFX client:load /> -->
  </div>

  {breadcrumbs && <div slot="breadcrumbs">{breadcrumbs}</div>}
</LayeredLayout>
